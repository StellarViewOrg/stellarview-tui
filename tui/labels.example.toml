# Custom labels for stellar-tui.
# Place this file in the current directory or at ~/.config/stellar-tui/labels.toml

[accounts]
# Simple name
# "G..." = "Treasury"

# Name plus searchable tags
# "G..." = { name = "Ops Wallet", tags = ["ops", "treasury"] }

[transactions]
# "abc123..." = "Initial deploy"

[contracts]
# "CC..." = "USDC SAC"

[assets]
# "USDC:GISS..." = "Circle USDC"