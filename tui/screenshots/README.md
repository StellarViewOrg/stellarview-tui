# Screenshots

Capture terminal screenshots after UI changes:

```bash
# Home screen
./bin/stellar-tui -no-interactive --screen home > /dev/null

# Lookup screen
./bin/stellar-tui -no-interactive --screen lookup > /dev/null
```

Save PNG captures from your terminal emulator into this directory for release notes and README embeds.